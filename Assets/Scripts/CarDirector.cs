using System.Collections;
using System.Collections.Generic;
using UnityEngine;

public class CarDirector : MonoBehaviour
{
    [SerializeField] List<GameObject> turnIndicators = new List<GameObject>();
    [SerializeField] List<GameObject> cars = new List<GameObject>();
    [SerializeField] CircuitGenerator circuitGenerator;
    // Start is called before the first frame update
    void Start()
    {
        
    }

    // Update is called once per frame
    void Update()
    {
        
    }
}
